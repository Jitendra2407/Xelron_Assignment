Read the file located at `/app/input.txt`.

Count the total number of lines and the total number of words in that file.

Write the result to `/app/output.txt` exactly in the following format:

lines: <number_of_lines>
words: <number_of_words>

Ensure you use absolute paths for both reading and writing.
